"""
Streamlit app — live demo of the emotion detection model.
To be completed after Phase 9 (model saving).
Run locally with: streamlit run app.py
"""
import streamlit as st

st.title("Emotion Detection from Text")
st.write("Model not yet trained — this is a placeholder. Coming in the deployment phase.")

# text = st.text_input("Enter a sentence:")
# if text:
#     label, confidence = predict_emotion(text)
#     st.write(f"Predicted emotion: **{label}** ({confidence:.0%} confidence)")
